{
  "buildCommand": "ng build --configuration production",
  "outputDirectory": "dist/initium-test",
  "installCommand": "npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
